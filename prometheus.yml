global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'travel-mcp-server'
    static_configs:
      - targets: ['travel-mcp-server:8000']
    metrics_path: '/metrics'
    scheme: 'http'
